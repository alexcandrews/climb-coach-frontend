# Environment configuration for the Climb Coach Expo app

# Public API base URL used by the mobile client and OpenAPI codegen script
EXPO_PUBLIC_API_URL=https://climb-coach-backend.example.com

# Supabase project details (public values safe to embed in the app bundle)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# After updating these values run the OpenAPI generator to refresh the typed client:
# npm run generate:api
